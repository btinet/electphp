{% extends 'base.html.twig' %}

{% block title %}Election{% endblock %}

{% block body %}
    <h1>Election</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ election.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ election.label }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ election.description }}</td>
            </tr>
            <tr>
                <th>Voices</th>
                <td>{{ election.voices }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_election_index') }}">back to list</a>

    <a href="{{ path('crud_election_edit', {'id': election.id}) }}">edit</a>

    {{ include('election/_delete_form.html.twig') }}
{% endblock %}
