{% extends 'base.html.twig' %}

{% block title %}Edit Election{% endblock %}

{% block body %}
    <div class="container pb-5">
        <div class="card">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center">
                    <div class="fw-bold">{{ election.label }} bearbeiten</div>
                    <a href="{{ path('crud_election_index') }}" class="btn btn-secondary">Zurück</a>
                </div>
            </div>
            <div class="card-body bg-light-subtle">
                {{ include('election/_form.html.twig', {'button_label': 'Aktualisieren'}) }}
                <div class="mt-3">
                    {{ include('election/_delete_form.html.twig') }}
                </div>
            </div>
            <div class="card-footer">
                <div class="text-muted small">
                    Wahlrunde bearbeiten
                </div>
            </div>
        </div>
    </div>
{% endblock %}
